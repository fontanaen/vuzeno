{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.x",
  "buildCommand": "bun run registry:build && bun run docs:build",
  "outputDirectory": ".vercel/output",
  "env": {
    "NODE_OPTIONS": "--max-old-space-size=8192"
  }
}
